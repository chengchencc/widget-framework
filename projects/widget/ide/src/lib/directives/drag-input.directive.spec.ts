import { DragInputDirective } from './drag-input.directive';

describe('DragInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DragInputDirective();
    expect(directive).toBeTruthy();
  });
});
